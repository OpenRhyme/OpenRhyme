// Tests for the Capture module. Use Swift Testing (`import Testing`) once tests exist.
//
// Anything that touches the live Accessibility API needs a TCC grant and cannot run
// on CI — keep such tests behind an explicit flag and unit-test the pure layers.
